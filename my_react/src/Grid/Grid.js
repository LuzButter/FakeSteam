import { Children, useEffect } from "react";

const Row = (
    Children,
    [
        gutter = '0px',
        span,
    ]) => {

    // useEffect = (gutter) ={

    // ,}

    return (
        <div
            style={{
                display: 'flex',
                flexWrap: 'wrap',
                marginRight: gutter,
                marginLeft: gutter,
                marginTop: gutter,
            }}>

        </div >
    )
}